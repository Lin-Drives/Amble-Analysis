# 安步 Amble —— MVP 产品定义文档

> **项目阶段**:Demo / MVP(最小可行产品)
> **文档关系**:本文档由《gait_analysis_elderly_project.md》(完整版愿景)精简而来。原文档保留作为长期路线图参考;本文档只定义「第一期能跑起来的 demo」。
> **核心原则**:越简单越好,能跑起来、能给真人用、能收到反馈,就是 MVP 的全部目标。

---

## 1. 定位与初衷

**起点**:大量中老年人存在步态退化、跌倒风险升高的问题,而普通家庭不具备早期识别和介入的专业知识。安步想用 AI 让步态/体态评估像测体温一样简单。

**定位调整(v0.2 更新)**:不再限定老年人群。年轻人同样有大量体态/步态问题(久坐、圆肩驼背、骨盆前倾、内外八),且年轻人**自己下载、自己付费、反馈即时**,是 demo 阶段最好的试验田。老年功能(跌倒风险、搀扶拍摄兼容)在 demo 验证后叠加。

**双品牌**:中文「安步」,英文「Amble」(留作国际化伏笔)。

**一句话定义**(当前版本):
> 用手机拍一段走路视频,30 秒内给你一份看得懂的体态/步态报告。

---

## 2. MVP 目标与成功标准

| 目标 | 成功标准 |
|------|---------|
| 技术上跑得通 | 任意手机拍 5 米走路视频 → 稳定提取骨架 → 算出指标,成功率 > 80% |
| 真人愿意用 | 10 个身边用户(朋友/家人/同事)完整走完全流程 |
| 收到真实反馈 | 每个试用者回答 3 个固定问题(见第 6 节) |
| 验证核心价值 | 用户看完报告后的第一反应是「原来如此」而不是「这准吗?」 |

**明确不是目标**:用户量、付费、医疗精度、临床验证——这些留给后面的阶段。

---

## 3. MVP 功能范围

### 3.1 唯一核心主线

```
拍摄引导 → 姿态提取 → 指标计算 → 一页式报告
```

**只有这一条主线,没有其他功能。**

### 3.2 拍摄引导(抄 LocoStep 的作业)

> **v0.3 决策:demo 只用正面拍摄。** 参考 LocoStep(日本 Class I 医疗器械)的做法——正面拍走向镜头,任何 5 米走廊 10 秒测完,落地性最强;矢状面关节角度留待后续版本(见调研/LocoStep_深度产品档案.md 第 8 节的取舍分析)。

- **正面拍摄**:手机固定,人**走向镜头**,直线走约 5 米(走廊即可)
- 拍摄时长 ≤ 20 秒,不需要用户学任何新动作
- 实时骨架叠加显示(让用户立刻看到"AI 在看我",建立信任感)
- 简单质检:人不在画面里/距离太近/光线太暗 → 文字提示重拍

### 3.3 指标(v1 只出 4 个,全部适配正面视角)

| 指标 | 计算来源 | 用户能懂的表达 |
|------|---------|---------------|
| **步速** | 行走距离(默认 5 米,可输入)÷ 行走用时 | 「你每秒走 X 米,比同龄均值快/慢」 |
| **左右对称性** | 左右步宽/摆动幅度比值(正面天然可见) | 「你的左腿和右腿发力均匀吗」 |
| **躯干摇晃** | 双肩中点的横向摆动幅度 | 「走路时身体左右晃吗」 |
| **步频** | 双踝横向间距的周期性变化 | 「每分钟 X 步」 |

指标选择原则:**全部可从单目 2D 姿态可靠计算**,不碰 3D、不碰动力学。每个指标附「正常参考范围」色带(绿/黄/红信号灯,抄 LINDERA 的 Ampel 设计)。

### 3.4 报告(一页式)

- 顶部:总体信号灯(绿/黄/红)
- 中部:4 个指标各一行,数字 + 参考范围色带
- 底部:**一条**可执行建议(例:「走路时躯干摇晃偏大,试试每天靠墙站 2 分钟」),只给一条,不淹没用户

### 3.5 明确不做的(v1 砍掉,防止范围蔓延)

- ❌ 肌肉力量分级(需要配对标注数据,见调研报告)
- ❌ 髋/膝/踝关节问题分型(需要矢状面 3D 精度,单目 2D 不可靠)
- ❌ 个性化训练处方库(先给「一条建议」,处方库等验证后再建)
- ❌ 账号系统、云端同步、历史趋势
- ❌ 多视角融合、跌倒风险评估模型
- ❌ 原生 App 上架(iOS/Android 商店流程太重)

> 以上功能在完整版愿景文档中都有规划,MVP 验证通过后再逐项捡回来。

---

## 4. 技术方案(最简栈)

| 模块 | 选型 | 理由 |
|------|------|------|
| 形态 | **网页 demo(PWA 思路)** | 手机浏览器打开即用,免安装免上架,发链接就能给人测 |
| 姿态估计 | **MediaPipe Pose(JS 版,端侧)** | 零成本、浏览器内实时、无需服务器;精度已被多项临床步态研究验证可用(见调研报告) |
| 指标计算 | 纯前端 JS | 步频/对称性/前倾角都是关键点坐标的几何计算 |
| 视频 | 前端直接调用摄像头,**不保存不上传** | 隐私零负担,也省掉后端 |
| 后端 | **无**(或仅一个静态托管) | MVP 不需要 |

**技术降级预案**:如果浏览器端 MediaPipe 在某些机型不稳,退回「录视频→上传→Python 脚本离线分析→返回报告图片」的半自动流程,demo 阶段完全够用。

---

## 5. 数据来源与验证

- **管线验证**:用公开数据集 GAVD(1,874 段带临床标注的步态视频,github.com/Rahmyyy/GAVD)跑通「视频→指标」管线,对比标注验证合理性
- **参考区间**:步速/步频的正常范围用公开文献值(步速 < 1.0 m/s 为跌倒高风险等循证阈值,见完整版愿景附录)
- **真人自采**:10 个试用者的视频(知情同意后)留档,作为第一批自有数据
- 详细数据集清单见《调研/公开步态数据集调研报告.md》

---

## 6. 反馈收集设计(MVP 的另一半)

给 10 个试用者测完后,固定问 3 个问题:

1. **看完报告,你记住了什么?**(测报告可理解性)
2. **你会想再测一次,或者让家人测吗?为什么?**(测留存/传播意愿)
3. **如果它能再告诉你一件事,你最想知道什么?**(收集下一期需求——验证「肌力/关节/训练建议」到底哪个是真需求)

附加观察:拍摄过程是否顺利、有没有人拍失败、老年用户能否在家属协助下完成全流程。

---

## 7. 迭代计划(4 周)

| 周 | 交付 |
|----|------|
| W1 | 网页 demo 骨架:摄像头调用 + MediaPipe 实时骨架叠加 + 录制 20 秒 |
| W2 | 指标计算:步频/对称性/前倾角/步速 + 步态周期自动分割(踝点速度法) |
| W3 | 一页式报告(信号灯+色带+一条建议)+ GAVD 公开视频回归验证 |
| W4 | 10 人试用 + 3 问反馈收集 → 输出《demo 复盘与下一期决策》 |

---

## 8. 与完整版愿景的对应关系

| MVP(v0.2 本文档) | 完整版愿景(gait_analysis_elderly_project.md) |
|-------------------|--------------------------------------------|
| 4 个 2D 几何指标 | ≥50 维步态特征向量(Phase 1) |
| 一条建议 | 训练处方库 + 难度分级(第 2.3 节) |
| 信号灯 | 跌倒风险等级 + 肌力分级 + 关节分型(第二层架构) |
| 网页 demo | iOS/Android 端侧 App(Phase 2) |
| 10 人试用反馈 | 200 例前瞻性队列 + 临床验证(第 4 章) |

MVP 的每一个「砍掉」都对应愿景中的一个「捡回」,验证什么、补什么,由第 6 节的反馈结果决定。

---

## 9. 边界声明

- demo 报告文案统一口径:「体态/步态参考信息,不构成医疗建议」
- 视频端侧处理、不上传,符合个人信息保护法
- 不涉及任何诊断表述,规避医疗器械监管边界(监管路径分析见《调研/LocoStep_深度产品档案.md》第 8 节)

---

*文档版本:v0.3(MVP 定义,demo 拍摄方式改为正面)*
*创建日期:2026-09-21*
*上游文档:gait_analysis_elderly_project.md(v0.1 完整版愿景)*
